# CMake build trees (iOS/desktop). SDL3 is fetched into build/*/\_deps.
build/

# PlatformIO artifacts, when this repo is built in place.
.pio/
compile_commands.json

.DS_Store

# Session-local agent worktrees (Claude Code). Transient working copies.
.claude/worktrees/
ios/seedfonts/
__pycache__/
build-simsdk/
# 473 MB CMake tree from the 2026-08-30 corner-radius sweep. Same class as
# build-simsdk/ above: a scratch configure dir, never part of the source.
build-rad/

# Generated: the packaged Mac bundles (deploy_mac_apps.sh) and the QA capture
# scratch. Both are build products of scripts in this repo, both are tens of MB,
# and both were untracked-but-not-ignored until 2026-08-23 -- so every git status
# offered to add 38 MB of binaries.
dist/
qa/

# Generated by `xcodegen generate` from tools/axprobe/project.yml. The recipe is
# in tools/axprobe/README.md; the project itself is derived and machine-local.
tools/axprobe/AXProbe.xcodeproj/
